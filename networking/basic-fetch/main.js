let textUrl = '';
let jsonUrl = '';
let imageUrl = '';



function fetchText(textUrl){
  return fetch().then().then().catch();
}

function fetchJson(jsonUrl){
  return fetch().then().then().catch();
}

function fetchImage(imageUrl){
  return fetch().then().then().catch();
}
